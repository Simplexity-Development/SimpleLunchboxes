name: SimpleLunchboxes
version: '${project.version}'
main: simplexity.simplelunchboxes.SimpleLunchboxes
api-version: '26.1'
permissions:
  simplelunchboxes.give:
    description: "Allows using /simplelunchboxes give"
    default: op
  simplelunchboxes.reload:
    description: "Allows using /simplelunchboxes reload"
    default: op
